# CRA / CRC 项目管理系统：商业分析与系统定位

> 视角：一名同时担任 CRA/CRC 项目经理与公司经营者的人，对"要不要做、做成什么样"的判断。
> 文档性质：决策文档，不是需求清单。所有行业数值为经验区间，必须用本公司真实数据校准后再用于决策。

---

## 0. 结论先行

1. **这套系统的本质不是"临床试验系统"，而是"人力密集型服务业的运营管理系统"。** CRO/SMO 卖的是人天，系统要管的是人、工时、成本、毛利和质量风险，不是受试者数据。
2. **第一阶段坚决不碰 GCP 受监管范围**（EDC / eTMF 主库 / eConsent / IWRS / 源数据）。碰了就要做计算机化系统验证（CSV），验证与合规成本通常达到开发成本的 1～2 倍，且每次迭代都要重新验证——对自建系统是死亡陷阱。
3. **最小作业单元是"项目 × 中心"（StudySite），不是"项目"。** 收入按中心计、成本按人落在中心上、进度按中心统计、核查风险按中心暴露。整个数据模型必须围绕这个交叉点建。
4. **优先级只有一条主线：先把工时和派工数据收上来 → 才算得出单中心成本 → 才算得出项目毛利 → 才谈得上科学报价。** 反过来先做报价模型，一定是拍脑袋。
5. **最大的失败风险不是技术，是"没人填"。** CRC 在医院现场、手机操作、网络差、时间碎片化。填报动作超过 30 秒、或者与本人绩效不挂钩，数据就一定是假的。

---

## 1. 商业诊断：这门生意的钱是怎么丢掉的

在决定做系统之前，先说清楚要解决的是哪几笔钱。

### 1.1 成本结构

| 业务线 | 主要成本项 | 经验占比 |
|---|---|---|
| SMO（CRC 服务） | CRC 人力（薪酬+社保+管理分摊） | 收入的 60%～70% |
| CRO 监查（CRA 服务） | CRA 人力 + 差旅 | 人力 50%～60%，差旅 10%～20% |
| 两者共同 | 管理层/QA/PM 分摊、招聘与流失成本 | 10%～15% |

含义很直接：**只要人天利用率波动 5 个百分点，毛利就会波动一大截。** 这是系统能创造价值的第一个杠杆。

### 1.2 三个典型的亏损来源

**（一）人效黑箱**
经营者回答不出这几个问题：
- 某个 CRC 现在同时带几个项目、几个中心？其中有几个是不计费的"帮忙"？
- 某个 CRA 上个月做了几次监查访视？差旅费摊到哪个项目？
- 下个月要启动 3 个新项目，我到底缺不缺人、缺几个、缺在哪个城市？

后果：报价靠拍脑袋、投标时不敢报低（丢单）也不敢报高（丢单），接下来项目才发现人不够，用加班和"一人多项"硬撑，质量随之下滑。

**（二）质量风险滞后暴露**
方案偏离、知情同意书版本用错、SAE 超窗上报、SDV 未完成、必备文件缺失——这些问题在**稽查或药监核查时才第一次被系统性看见**。而那时的代价是：单中心数据被剔除、整个项目延期、申办方索赔、公司在申办方合格供应商名录上降级。

系统的价值不是"事后记录"，是**把风险信号提前到日常作业里**：访视窗口临期提醒、SAE 上报倒计时、质疑（query）超期未关闭清单、GCP 证书到期预警。

**（三）回款慢、收入确认没有证据链**
SMO/CRO 的收入通常绑定里程碑（伦理批件、中心启动 SIV、首例入组 FPI、入组过半、末例出组 LPO、数据库锁定 DBL）。但这些里程碑的证据散落在邮件、微信群和个人电脑的 Excel 里。开票要人肉翻记录，账期被拖长，DSO（应收账款周转天数）居高不下。

### 1.2b 一个必须承认的前提：这既是管理系统，也是干活系统

上面三条都是从经营者视角写的。但如果系统只服务经营者，它会以最直接的方式失败：
**一线不填，或者填假的。**

管理层要的数（人效、毛利、报价）全部来自一线的日常录入。而一线只会认真使用**帮他干活**的工具。
所以这套系统必须同时成立两件事：

- 对经营者：它是算账的系统；
- 对 CRA/CRC：它是**干活的系统** —— 访视窗口不用自己记、质疑不用翻邮件、药品数量自动平衡、
  补偿发放有台账、工时随手就填完。

**这不是"顺便照顾一下员工体验"，而是数据能不能收上来的前提条件。**
员工端做成管理端的只读切片加几个按钮，等于在源头上把整套系统架空。

### 1.3 为什么不直接买现成产品

市面上的 CTMS/EDC（Medidata、Oracle、以及国内的太美、易迪希等）**是按申办方视角设计的**：它们关心的是"这个试验做到哪一步、数据干不干净"。

而 CRO/SMO 经营者关心的是**另一组完全不同的问题**："我的人有没有闲着、这个项目还赚不赚钱、下次报价该报多少"。

这不是功能差异，是立场差异。申办方视角的系统天然不会帮乙方算毛利。**这就是自建的空间：不去和 EDC 厂商正面竞争，去做他们不做的那一半——我自己的人和我自己的钱。**

---

## 2. 战略边界：做什么，坚决不做什么（本文档最重要的一节）

### 2.1 边界图

```
┌──────────────────────────────────────────────────────────┐
│  受监管区（GCP / 21 CFR Part 11 / 数据完整性 ALCOA+）      │
│  EDC · ePRO · eConsent · IWRS/随机化 · eTMF主库 · 源数据   │
│  → 第一阶段不做。只做「接口预留」和「状态同步」            │
└──────────────────────────────────────────────────────────┘
                          ▲  只读引用 / 状态回写
┌──────────────────────────────────────────────────────────┐
│  ★ 本系统作战区：运营管理层（CTMS-lite + 人力运营）        │
│  项目·中心台账 │ 派工与产能 │ 工时与差旅 │ 里程碑与结算    │
│  质量事件与CAPA │ 监查访视管理 │ 经营看板与报价模型        │
│  —— 只存元数据，不存任何受试者可识别信息 ——               │
└──────────────────────────────────────────────────────────┘
```

### 2.2 为什么必须划这条线

| 维度 | 受监管区（EDC/eTMF 等） | 运营管理层（本系统） |
|---|---|---|
| 监管要求 | GCP、数据完整性、电子签名、审计追踪 | 一般企业信息系统要求 |
| 上线前提 | 必须完成 CSV：URS/IQ/OQ/PQ 全套验证文档 | 内部 UAT 即可 |
| 每次迭代 | 变更控制 + 回归验证，周期以月计 | 正常敏捷迭代 |
| 一旦出错 | 数据可能被监管否定，项目重做 | 内部管理损失，可修复 |
| 竞争格局 | 成熟厂商林立，客户切换成本极高 | 基本空白，各家用 Excel |
| 建议 | **买/对接，不自建** | **自建，这是护城河** |

一句话：**受监管的部分，出事的代价是"项目死"；运营管理的部分，出事的代价是"改个数"。用有限的研发资源去承担前者的风险，是经营上的错误决策。**

### 2.3 数据红线（架构层面强制，不是靠制度约束）

本系统数据库中**不得出现**：受试者姓名、身份证号、住院号/门诊号、联系方式、出生日期（精确到日）、原始病历内容、知情同意书扫描件。

只允许存在：
- 受试者**筛选号 / 随机号**（且优先只存**计数**，如"该中心已入组 12 例"）
- 访视**窗口日期**（用于排班），不与个人身份关联

理由：一旦系统内出现可识别的患者信息，就同时落入《个人信息保护法》敏感个人信息 + GCP 受试者隐私保护 + 申办方数据保密协议三重管辖，安全投入和法律风险呈量级上升，而这些信息对"管人管钱"毫无用处。**不需要的数据，最好的保护方式是不采集。**

#### 修正：红线不是"不存姓名"，是"不可重新识别"

上面这条最初写成"不存姓名/证件号"，这个判断**不够严谨**。

要做 CRC 端的访视窗口管理，系统里必然同时存在：筛选号 + 具体访视日期 + 医院 + 科室 + 该次访视的检查项。
在一个只入组 26 例的中心，「S-0217 · 北京协和 · 肝胆外科 · 2026-08-21 · 3 级肝功能异常 AE 随访」
这组**准标识符（quasi-identifier）组合**足以定位到具体患者。按《个人信息保护法》，这已构成
"通过与其他信息结合识别特定自然人"，且属健康信息（敏感个人信息）。

所以红线要重写为三条可执行的约束：

1. **不采集直接标识符**（姓名、证件、住院号、联系方式、精确出生日期、原始病历、ICF 扫描件）—— 保持不变。
2. **准标识符组合按敏感个人信息管理**：访视日期 + 中心 + 科室 + 事件描述的组合视为可间接识别，
   适用最小必要、访问留痕、按角色收敛的要求；跨中心的聚合视图只允许出现**计数**，不允许出现单条受试者记录。
3. **可见范围按角色硬切**：CRC 只看本人负责的受试者，PI 只看本中心，机构办只看本院且不看事件明细以外的内容，
   经营层与 PM **完全看不到单条受试者记录**，只看聚合计数。

第 3 条是关键 —— 老板不需要看到任何一个受试者，他只需要知道"这个中心入组 26 例"。
**把可识别风险挡在角色边界上，比挡在字段清单上更可靠。**

---

## 3. 领域模型：以 StudySite 为核心

### 3.1 核心实体

```
Sponsor 申办方
  └── Study 项目（方案号/适应症/期别 I-IV/预算/计划例数/起止）
        └── ★ StudySite 项目中心【最小作业单元】
              ├── Site 中心（医院/科室/PI/伦理委员会）
              ├── 状态机：立项→伦理递交→伦理批件→合同→SIV启动
              │           →入组中→入组完成→随访中→关闭 COV
              ├── Enrollment 入组进度（计划曲线 vs 实际，只存计数）
              ├── Assignment 派工（Staff × 角色 × FTE × 起止）
              ├── VisitTask 任务（CRC 受试者访视陪同 / CRA 监查访视 SIV·IMV·COV）
              ├── Timesheet 工时（人天，归集到本 StudySite）
              ├── Expense 差旅费用（与监查访视绑定）
              ├── Milestone 里程碑（→ Invoice 开票 → Payment 回款）
              └── Issue 质量事件（方案偏离 / 稽查发现 / query）→ CAPA

Staff 人员（CRA/CRC/PM/QA）
  ├── 资质：GCP 证书及有效期、治疗领域经验、常驻城市
  ├── 成本费率（内部人天成本）
  └── 产能：可用人天 / 已派工 FTE 合计
```

### 3.1b 受试者是有生命周期的对象（原分析遗漏，此处补上）

原模型里入组只有一个标量 `enrolled`。这是错的：从"没这个人"到"入组 +1"之间有一整条漏斗，
而且受试者入组之后还要按访视计划表（SOA）走十几次访视，直到出组。

```
预筛 → 签署 ICF → 筛选期检查 → 入组（随机号）/ 筛败
                                    ↓
                        按 SOA 逐次访视 → EDC 录入 → 研究者确认 → 出组
```

两条推论：

- **受试者不能"做完一次访视就消失"。** 完成 C6D1 之后，系统应按 SOA 自动生成 C7D1 的窗口。
  访视是对象的状态推进，不是一次性待办。
- **访视与工时必须是同一次录入。** 分两次录（先完成访视、再单独填工时）看似灵活，
  代价是**"每次访视实际耗时"这个数永远算不出来** —— 而它恰恰是报价模型最需要的输入。
  P0 收数据是为了 P2 报价，这条链断在这里就白收了。

### 3.1c 筛败漏斗：这笔账原来在源头上就是错的

**这是本文档最严重的一处修正。** 原模型里：

```
收入 = 启动费 + 入组例数 × 单例单价          ← 漏了筛败费
成本 = 工时归集 + 差旅 + 分摊                ← 工时里没有筛败例的筛选期工作量
```

行业筛败率 30%–50%，肿瘤 II/III 期常见 50%+。这意味着两个方向相反的错误：

| | 原口径 | 实际 |
|---|---|---|
| **成本** | 只算入组受试者的访视陪同 | CRC 有相当比例的工作花在最终筛败的人身上，被系统性低估 |
| **收入** | 只有启动费 + 入组费 | 合同通常有**筛败费**（screen failure fee，入组单价的 30%–40%） |

两个错误方向相反，最终毛利率错在哪都定位不了。**以"算清每个中心赚不赚钱"为核心命题的系统，
最核心的那笔账在源头上是错的。** 补上筛败费后（按入组单价 35% 计），演示数据里收入增加约 14%，
被误判为亏损的中心从 5 个减少到 4 个 ——
**漏算筛败费会让高筛败率中心被误判为亏损，从而砍掉本来赚钱的中心。**

还有一个谈判层面的用处：同样的总价可以有两种拆法。

> 合同含筛败费 → 入组单价可以报低；合同不含 → 入组单价必须顶回去。
> 申办方砍筛败费时，这就是把单价顶回去的量化依据。

#### 落后中心的三种失败模式

"入组落后"这个结论本身没有行动价值，答案藏在漏斗前面三格里：

| 模式 | 特征 | 该做的事 |
|---|---|---|
| **预筛量不足** | 筛败率正常，但累计预筛远低于合同例数所需 | 病源本身不够 —— 这是**可行性调查**该拦下的中心 |
| **筛败率过高** | 预筛充足，但筛败超过 50% | 入排标准与该中心病源不匹配 → 谈方案修订，或降低该中心合同例数 |
| **中心未真正启动** | 预筛个位数，实质未开展 | PI 参与度问题 → PM 出面，否则关闭中心止损 |

同样是"入组落后"，三种模式的对策完全不同。而筛败**原因分布**又进一步指向具体动作：
实验室指标不符占比高 → 入排窗与本土人群不匹配；受试者撤回知情占比高 → 知情沟通或补偿方案有问题，这是 CRC 可控的。

### 3.2 为什么核心是 StudySite 而不是 Study

这是建模上最容易犯错、也最致命的一处。以"项目"为核心的系统，做出来的报表全是无效的。原因：

| 业务事实 | 归属层级 |
|---|---|
| SMO 报价 = 中心数 × 例数 × 单价 | **中心** |
| 里程碑付款（SIV、FPI）逐中心触发 | **中心** |
| CRC 派驻是驻某个医院，不是驻某个项目 | **中心** |
| 入组快慢差异巨大（同项目不同中心可差 10 倍） | **中心** |
| 药监核查是抽查到具体中心 | **中心** |
| 亏损项目往往是被 2～3 个"零入组中心"拖垮 | **中心** |

**"项目整体毛利 15%" 这个数字毫无经营价值；"A 医院单中心毛利 40%、B 医院 -20%" 才是可以据以行动的信息。**

### 3.3 一个必须内建的规则：计费 vs 不计费

工时必须强制区分：
- **可计费（Billable）**：合同范围内的中心作业
- **不可计费（Non-billable）**：内部培训、投标支持、无合同的"帮忙"、返工

不区分，利用率指标就是假的。而经验上，**未识别的不可计费工时，正是毛利蒸发的主要去处**。

### 3.4 系统的入口：立项与建档（原分析遗漏，此处补上）

前面把"管什么"讲清楚了，却漏了一件更基础的事：**这些数据是怎么进来的。**

没有录入入口的系统，等于要求所有人先在别处把数据攒好再誊一遍 —— 这是 Excel 永远赶不走的真正原因。
入口必须是两段：

| 环节 | 谁做 | 关键字段 | 为什么在这里卡一道 |
|---|---|---|---|
| **项目立项** | PM 提交 / 经营层审批 | 方案号、申办方、期别、适应症、中心数、计划例数、合同额、周期、里程碑定义 | **立项时就用报价模型的口径算一次毛利率**。低于门槛（建议 25%）必须经营层签字 |
| **中心建档** | PM 或 CRA | 医院、科室、PI、合同例数、单例单价、伦理路径、机构受理号、派驻 CRA/CRC | 建档即进入状态机第一节点；派驻时同步做冲突检测（超载、跨城驻场、GCP 临期） |

**立项测算必须和报价模型共用同一套人天口径。** 两套账是这类系统最常见的溃败方式：
商务用一套参数报价，交付用另一套算成本，事后对不上，谁也说不清项目是怎么亏的。

还有一个容易被忽略的指标：**「合同中心数」与「已建档中心数」的差**。
差值代表合同里写了、但还没进系统的中心 —— 它们的成本（伦理递交、合同谈判、启动准备）已经在发生，
收入却还挂不上号。**建档滞后是早期成本失控最常见的一种，而它在任何报表上都不会自己冒出来。**

### 3.4b 中心可行性调查：选址是选址，事后标红不是

原分析花了大量篇幅讲"亏损项目往往被 2–3 个零入组中心拖垮"，也设计了把这些中心标红的报表 ——
**却完全没有讲这些中心当初是怎么被选进来的，也没有任何机制防止下次再选错。**

这是原分析里最讽刺的一处遗漏。**事后标红是最便宜的功能，也是最没用的功能。**

可行性调查（feasibility）该沉淀的字段：年同适应症病源量、既往同类试验的**实际**月入组、
在研竞争试验数、伦理与启动周期、研究团队规模、PI 承诺月入组。三条设计要点：

1. **PI 承诺必须打折。** 行业经验兑现率 40%–60%。直接用承诺值排产能，是启动期人力估算失准的头号原因。
   预测月入组 = min( PI 承诺 × 兑现系数，病源上限 )。
2. **必须做回溯。** 不回溯的可行性调查只是一张走过场的表。把"当初预测"和"实际月入组"放在一起，
   才能算出本公司的系统性偏差系数。
3. **必须记录 override 理由。** 评分不达标却仍然入选时，强制写明理由并永久留痕，
   然后统计"被覆盖入选的中心实际表现如何"。
   演示数据里的两条理由是"申办方指定"和"为赶 FPI 凑中心数"——
   **这两个理由最后都由我们自己买单。记下来，下一次谈判时它才是筹码。**

问卷本身也应当随复盘进化。演示数据里湘雅二病源充足、评分不低，却出现 57% 筛败率 ——
因为当时的问卷**只问了门诊量，没问"按本方案入排标准能过几个"**。
"入排合格率"是复盘之后才加进问卷的字段。

**可行性库与报价模型是并列的两条护城河：报价模型算"要花多少人天"，可行性库算"这家能不能出病人"。**

### 3.4c 法定角色：PI、QA、数据管理

原模型的实体清单里 PI 只是一个字符串。但在 GCP 框架下，**研究者是法定责任人**：
方案偏离要 PI 签字、SAE 由 PI 上报、授权分工表由 PI 签署。
CRC 完成一次访视后没有研究者确认环节，在合规上说不通 —— 现实中这一步是要签字的。

三个必须建模的角色：

| 角色 | 归属 | 在系统里必须能做的事 |
|---|---|---|
| **PI 研究者** | 医院 | 确认并签署访视记录、签署方案偏离、上报 SAE、维护授权分工表 |
| **QA 质量负责人** | 我方 | 内部稽查、**CAPA 有效性验证**、中心质量评级 |
| **DM 数据管理** | 我方或申办方 | 发起数据质疑（原模型里 query 凭空出现，没有入口） |

其中 QA 的价值最容易被误解。**QA 不是再发现一批问题，而是验证 CAPA 到底防住了没有。**

> 演示数据里「源数据缺陷」的 CAPA 写的是"研究者集中补签并留痕"——
> **那是纠正，不是预防。** 补完签名，下个月照样缺，稽查时果然复发。
> 真正的预防应当是把签名完整性做进 CRC 每周自查清单并留痕，或改用带强制签名字段的电子源数据。
> **只做纠正不做预防的 CAPA，等于把同一个核查风险往后推了一个季度。**

因此中心质量评级里，**"CAPA 关闭后复发"的权重应当最高** —— 它证明的是体系失效，不是单点失误。

### 3.5 外部协作方：机构办公室（原分析遗漏，此处补上）

§3.1 的实体清单里只有"我方的人"。但临床试验的质量责任链上，还有一个绕不开的角色：
**医院药物临床试验机构办公室的项目管理员（业内称"机构老师"）**。

他不是 CRO/SMO 的员工，是**医院方的守门人与质量责任人**，管四件事：

1. **立项受理**：受托方递交材料 → 形式审查 → 受理并转伦理。材料不齐就受理，后面每个环节都带着这个缺口往下走。
2. **机构质控**：一级质控（研究者自查）、二级质控（机构抽查）。发现问题 → 出具质控意见 → 派发整改 → 复核关闭。
3. **人员备案与准入**：CRC 由 SMO 派驻、CRA 由 CRO 派驻，但他们进的是**医院**的门、看的是**医院**的病历。
   谁能接触受试者源数据是医院的责任 —— GCP 证书未提供、方案培训未完成、未列入授权分工表的人，机构有权拒绝其开展工作。
4. **文件归档与项目关闭**。

**这个角色补上以后，质量事件才真正闭环。** 此前的模型里，质量事件只有"我方发现、我方整改、我方关闭"，
这是自己给自己打分。真实的链条是：**机构质控发现 → 出具意见 → 受托方整改回复 → 机构复核关闭** ——
关闭权在医院，不在我方。

由此还引出一个原分析没写透的指标：**问题的发现来源分布**。

> 前置发现（中心自查、机构质控、内部监查）占比越高，说明质量防线在起作用。
> 如果问题主要由**申办方稽查或药监核查**发现，代价就不再是整改，而是数据被剔除、项目延期、供应商名录降级。
> **发现来源的分布，比未关闭事件的绝对数量更能说明质量体系是否在运转。**

#### 边界：外部视图的权限比内部员工更窄，不是更宽

这一点很容易做反。机构老师在医院里级别不低，但在**我们的系统里**他只能看到：

- ✅ 本院承接的项目与中心、入组计数、质量事件、人员资质、文件状态
- ❌ 任何成本、毛利、人天、人员利用率、报价 —— 这是受托方的商业信息，与机构履行监管职责无关
- ❌ 其他医院的任何数据

反过来也一样：机构的内部质控底稿不向受托方开放全文，只发送质控意见与整改要求。
**把外部协作方的权限设计成"内部员工的子集"是错的 —— 它是另一个维度的切片：
按机构切行，按"质量与合规"切列。**

同理还有两个未在本期建模、但迟早要面对的外部方：**申办方**（要看进度与质量，不看我方成本）、
**伦理委员会**（只看伦理相关文件与 SAE）。架构上应把"外部协作视图"做成一类，而不是逐个打补丁。

---

## 4. 功能路线图：三期，每期都要能独立产生回报

### P0（约 0～3 个月）——"看得见"

目标：把 Excel 从关键流程里赶出去，把最基础的数据收上来。

0. **立项与建档入口**（见 §3.4）：项目立项（含毛利门槛审批）与中心建档。没有入口，后面九项都是空的。
1. **项目 / 中心台账 + 状态机**：所有项目中心的当前状态、关键日期、PI 与联系人、伦理与合同节点。
2. **入组进度看板**：计划曲线 vs 实际曲线，逐中心。入组延迟是临床试验项目亏损的第一大原因，这张图必须每天有人看。
3. **人员派工与产能视图**：谁、在哪个项目中心、什么角色、FTE 占比多少、时间区间。一眼看出谁超载、谁闲置。
4. **移动端工时与差旅填报**：CRC/CRA 在手机上完成，目标 **30 秒内提交**，支持弱网离线暂存。
   **工时必须与访视同一次录入**（见 §3.1b），否则"每次访视实际耗时"永远算不出来。
4b. **筛选漏斗**（见 §3.1c）：预筛 / 签署知情 / 筛败 / 入组 四格计数与筛败原因。
   没有这一格，收入口径是错的，入组落后也归不出因。
4c. **CRC 日常三件**：EDC 录入进度（访视后 5 个工作日内为及时线）、受试者联系与改期状态、受试者补偿发放。
   **系统不管的工作不会消失，只会变成没人记录的加班** —— 而这三件恰恰是 CRC 每周耗时最多的。

> P0 的回报：第一次拿到真实的人员饱和度分布。仅此一项，通常就能发现 5%～15% 的隐性闲置或隐性超载。

### P1（约 3～6 个月）——"算得清"

5. **成本自动归集**：人天成本 × 工时 + 差旅 → 自动落到 StudySite。
6. **里程碑 → 开票 → 回款闭环**：里程碑达成自动生成证据链和开票提醒，跟踪 DSO。
7. **单项目 / 单中心 P&L**：预算 vs 实际，实时毛利。亏损中心自动亮红灯。

> P1 的回报：从"项目结束后半年财务才告诉我亏了"，变成"项目进行到 40% 时就知道要亏，还来得及和申办方谈变更"。

### P2（约 6～12 个月）——"防得住"＋"报得准"

8. **质量事件与 CAPA 台账**：方案偏离、稽查发现、SAE 上报及时性、query 关闭时长；风险信号前置提醒。
   每条事件必须带**发现来源**（中心自查 / 机构质控 / 内部监查 / 申办方稽查 / 药监核查），并单列来源分布（见 §3.5）。
8b. **机构协作视图**（见 §3.5）：立项受理、机构质控意见与整改闭环、人员备案准入。按机构切行、按质量与合规切列。
9. **监查访视管理**：访视计划、MVR（监查报告）提交与跟进项闭环、必备文件完整性 checklist。
10. **★ 报价模型**：用历史真实人天数据反推报价基准——不同适应症、不同期别、不同中心等级的单例实际消耗人天。

> **第 10 项是最终的护城河。** 竞争对手报价靠经验，你报价靠自己三年的真实成本数据库。这是别人买不到、也抄不走的资产。而它必须建立在 P0 的数据之上——这就是为什么顺序不能倒。

---

## 5. 经营看板：老板每天该看的十个数

| # | 指标 | 定义 | 经验健康区间 | 为什么看 |
|---|---|---|---|---|
| 1 | 人员利用率 | 可计费人天 / 可用人天 | 70%～80% | 低了浪费，长期高于 85% 意味着质量与流失风险 |
| 2 | 单中心毛利率 | (中心收入-中心成本)/中心收入 | — | 识别亏损中心，及早止血 |
| 3 | 入组达成率 | 实际入组 / 计划入组（按时点） | ≥ 90% | 延期＝成本持续燃烧、里程碑收入推迟 |
| 4 | 每例入组成本 | 中心总成本 / 入组例数 | 与报价单价对比 | 直接判断这个中心该不该继续投人 |
| 5 | 人均带中心数 | CRA / CRC 各自统计 | CRA 约 8～12 个中心 | 超载是质量事故的先行指标 |
| 6 | 首例入组周期 | 伦理批件 → FPI 天数 | 越短越好 | 衡量启动效率，也是申办方最看重的能力 |
| 7 | 不可计费工时占比 | 非计费人天 / 总人天 | — | 毛利蒸发的隐藏出口 |
| 8 | SAE 24h 上报及时率 | 按时上报 / 全部 SAE | **必须 100%** | 合规红线，不达标即为重大风险 |
| 9 | Query 平均关闭天数 | — | 越短越好 | 数据质量与 CRC 战斗力的直接体现 |
| 10 | 逾期未开票金额 / DSO | — | — | 现金流生命线 |

> 建议：1～5 为经营例会必看；6～9 为质量例会必看；10 为财务周会必看。**看板不是为了好看，每个数字后面必须能对应一个具体的人和一个具体的动作。**

---

## 6. 合规与安全底线

即使避开了 GCP 受监管系统，仍有不可省的部分：

1. **数据最小化**：见 §2.3 红线。
2. **项目级隔离**：CRA 只能看到自己被派工的项目。申办方项目信息是商业机密，交叉泄露一次，公司就会被踢出合格供应商名录。
3. **审计日志**：入组数、里程碑达成日期、工时这三类数据的任何修改都要留痕（谁、何时、改前改后）。这既是内控，也是未来若要升级为受监管系统的必要基础。
4. **权限最小化 + 离职即时回收**：CRC/CRA 流动率高，权限回收必须与 HR 流程联动。
5. **《数据安全法》《个人信息保护法》合规**：如涉及境外申办方，还需评估数据出境要求。
6. **架构预留但不自建**：为未来对接 EDC / eTMF 预留接口层（只做状态同步与文件索引，不做主库）。

---

## 7. 落地风险：技术不是主要矛盾

按失败概率从高到低排列：

| 风险 | 概率 | 应对 |
|---|---|---|
| **一线不填 / 乱填数据** | 极高 | ① 移动端 30 秒完成；② 弱网离线；③ **工时与本人绩效奖金直接挂钩**；④ 上级审批而非事后稽核 |
| 数据脏导致报表不可信 | 高 | 先在 1～2 个项目试点，跑通一个完整季度的"派工→工时→结算"闭环再推广 |
| 一次上全，功能过重 | 高 | 严格按 P0/P1/P2 分期，每期必须有可衡量回报才启动下一期 |
| 与财务口径打架 | 中 | 建系统前先与财务统一"人天成本""收入确认时点"的定义，写进文档 |
| 项目经理抵触（透明化） | 中 | 定位为"帮 PM 要资源的证据"，而不是"监控 PM 的工具"——这一点的沟通方式决定成败 |

**关于最后一点的补充：** 系统会让每个 PM 的项目毛利变得公开可见。如果先把它包装成考核工具，会立刻遭遇集体软抵抗（数据照填，但全是精心修饰过的）。正确的引入方式是先用它帮 PM 拿到人手和预算，让 PM 先尝到甜头，考核属性半年后再逐步加载。

---

## 8. 自建 vs 采购：经济性测算框架

先回答"要不要自己做"，再谈怎么做。

**判断阈值：**

| 公司规模 | 建议 |
|---|---|
| 一线人员 < 50 人 | **不要自建。** 用飞书多维表格 / 钉钉 + 标准化 Excel 模板先把流程跑通，成本几乎为零 |
| 50～100 人 | 低代码平台搭建，或采购通用项目管理工具做二次配置 |
| > 100 人且多项目并行 | **值得自建**，尤其是想把"报价模型"做成护城河的公司 |

**测算逻辑（以 100 人规模为例，需用自家数据代入）：**

- 收益侧：
  - 人效提升 5% ≈ 释放 5 个人的产能
  - 亏损中心提前 2～3 个月识别 → 单个项目止损
  - 报价准确度提升 → 减少"报低了硬做"的亏损单
  - DSO 缩短 → 现金流改善（这一项对老板往往比毛利更实在）
- 成本侧：
  - 开发投入（P0+P1 约需一个 4～6 人的小团队半年）
  - **推行成本（常被严重低估）**：培训、试点、流程改造、持续运营
  - 长期维护

**关键提醒：** 上面的收益数字全部是待验证的假设。**做这个测算的目的不是算出一个漂亮的 ROI 去说服自己，而是逼自己写下"如果三个月后人效没有提升，我承认判断错误"的验收标准。**

---

## 9. 技术选型（简要，不是本文重点）

- **前端**：Web（管理端）+ 移动端。移动端优先考虑企业微信/飞书应用或小程序——**因为 CRC 在医院里用手机，不会开电脑**。这一条决定了整个交互设计。
- **后端**：单体架构起步（Spring Boot / NestJS 均可），不要一上来上微服务。业务复杂度不在并发，在领域模型。
- **数据库**：关系型为主。核心是把 §3 的模型建对，尤其是 StudySite 这张表的外键关系。
- **报表**：读写分离，报表走独立只读库或物化视图，避免拖垮业务库。
- **多租户/隔离**：即使只服务自己公司，也要按项目做行级权限，为将来对外输出（把系统本身做成产品卖给同行）留后路。

---

## 10. 如果我是老板，第一步做什么

**不是写代码。**

第一步是：**用 Excel，把"项目 × 中心 × 人 × 人天"这张表在两个真实项目上跑满一个季度。**

要验证的只有一件事：**这些数据到底收不收得上来。**

- 收得上来 → 说明流程和人的意愿没问题，系统只是把它自动化，值得投入。
- 收不上来（漏填、瞎填、拖两周才填）→ 那么问题在管理机制，不在工具。**这时候上系统，只会得到一个填满假数据的昂贵数据库，并且从此失去所有人对"数据"这件事的信任——这个信任一旦丢掉，第二次再推就没人配合了。**

数据收不上来，系统再好也是空的。这是我作为经营者最想守住的一条纪律。

---

## 附：术语

| 缩写 | 全称 | 说明 |
|---|---|---|
| CRA | Clinical Research Associate | 临床监查员，代表申办方/CRO 监查中心 |
| CRC | Clinical Research Coordinator | 临床研究协调员，在中心协助 PI 开展试验 |
| CRO | Contract Research Organization | 合同研究组织 |
| SMO | Site Management Organization | 中心管理组织，提供 CRC 服务 |
| PI | Principal Investigator | 主要研究者 |
| GCP | Good Clinical Practice | 药物临床试验质量管理规范 |
| CTMS | Clinical Trial Management System | 临床试验管理系统 |
| EDC | Electronic Data Capture | 电子数据采集系统 |
| eTMF | electronic Trial Master File | 电子试验主文件 |
| CSV | Computerized System Validation | 计算机化系统验证 |
| SIV / IMV / COV | Site Initiation / Interim Monitoring / Close-Out Visit | 启动 / 例行监查 / 关闭访视 |
| FPI / LPO / DBL | First Patient In / Last Patient Out / Database Lock | 首例入组 / 末例出组 / 数据库锁定 |
| SDV | Source Data Verification | 源数据核查 |
| SAE | Serious Adverse Event | 严重不良事件 |
| CAPA | Corrective and Preventive Action | 纠正与预防措施 |
| ICF | Informed Consent Form | 知情同意书 |
| ISF | Investigator Site File | 研究者文件夹 |
| DOA | Delegation of Authority | 研究者授权分工表 |
| 机构办 | — | 医院药物临床试验机构办公室 |
| SOA | Schedule of Assessments | 访视计划表 |
| SFR | Screen Failure Rate | 筛败率 |
| DM | Data Management | 数据管理 |
| PK | Pharmacokinetics | 药代动力学 |
| FTE | Full-Time Equivalent | 全职当量 |
| DSO | Days Sales Outstanding | 应收账款周转天数 |
