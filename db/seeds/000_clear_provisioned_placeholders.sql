/* ══════════════════════════════════════════════════════════════════════
   清掉开户占位的业务配置，给演示数据让路。

   ── 为什么需要这一步 ──────────────────────────────────────────────
   迁移 0024 让 `provision_tenant` 一并铺开业务配置（启动清单模板、
   费率卡基线），并给**已有租户**补了一次。而迁移总是跑在种子之前 ——
   于是本地与 CI 上的顺序是：

     migrate up  → 演示租户拿到 5 张占位费率卡（valid_from = 今天，开区间）
     seed 001    → 演示数据插入自己的费率卡（valid_from = 2026-01-01，开区间）
                   ↑ 两个开区间必然重叠，撞在 rate_card_no_overlap 上

   那条排他约束是对的：区间重叠时"当天用哪个费率"没有答案。
   所以要让路的是**占位的那一份**，不是约束。

   只删带着占位标记的行 —— 客户改过的、种子插的，一行都不碰。
   生产环境不跑种子，所以这个文件在那里根本不会被执行：
   占位费率卡留在那儿等运维改掉，正是它存在的意义。
   ══════════════════════════════════════════════════════════════════════ */

DELETE FROM rate_card WHERE note LIKE '开户占位费率%';
